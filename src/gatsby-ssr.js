import React from "react";

export const onPreRenderHTML = require('./preRenderHTML');
export const onRenderBody = require('./renderBody');
export const replaceRenderer = require('./replaceRenderer');
